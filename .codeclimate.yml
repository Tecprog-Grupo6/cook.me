engines:
  rubocop:
    enabled: true
  duplication:
    enabled: true
    config:
      languages:
        ruby:
          mass_threshold: 32
ratings:
  paths:
    - app/**
    - "**.rb"
exclude_paths:
- test/**
