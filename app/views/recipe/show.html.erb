<center>
  <div class="col-md-offset-4 col-md-4">
    <div class="row">
      <h3><%= @recipe_posted[:title]%></h3>
    </div>

    <div class="row">
      <div class="col-md-offset-2 col-md-8">
        <%= image_tag(@recipe_posted.image_one.url, class:"img-responsive") %>
      </div>
    </div>
    <div class="row">
      <p>Serve: <%= @recipe_posted[:served_people]%></p>
    </div>
    <div class="row">
      <p>Tempo de preparo: <%= @recipe_posted[:prepare_time]%></p>
    </div>
    <div class="row">
      <p>Modo de preparo: <%= @recipe_posted[:text]%></p>
    </div>
    <div class="row">
      <%= form_for :recipes, url: "/receita/deletar/#{params[:recipe_id]}" do |f| %>
        <div>
          <%= f.submit "Deletar receita", class: "btn btn-default submit" %>
        </div>
      <% end %>
    </div>
</center>
